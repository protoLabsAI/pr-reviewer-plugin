pytest>=8
pytest-asyncio>=0.24
pyyaml>=6
langchain-core>=0.3
ruff==0.16.2       # PINNED: CI and the board's local gate must resolve the SAME ruff (bd-7z5)
fastapi>=0.115
httpx>=0.27
cryptography>=42
langchain>=1.0,<2     # test-only: drives the real create_agent/langgraph step accounting (tests/test_structural_budget.py, #119)
